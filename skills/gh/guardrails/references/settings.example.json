{
  "//": "Commit this into the TARGET repo's .claude/settings.json so the irreversible-op guard runs in an unattended session (including a Cloud Routine's fresh clone). A local-only ~/.claude/settings.json does NOT propagate to a cloud routine. Adjust the script path to where gh-guardrails is installed in the target repo. Verify the routine actually honors the hook with a probe Run-now; if it does not, rely on withheld capability (no merge scope / no write connector).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python skills/gh/guardrails/scripts/guard_irreversible.py"
          }
        ]
      }
    ]
  }
}
